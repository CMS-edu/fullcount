services:
  - type: web
    name: fullcount
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: fullcount-db
          property: connectionString

databases:
  - name: fullcount-db
    plan: free
