// If the frontend is served by Render together with server.mjs, keep this empty.
// If the frontend is served by Vercel, set this to the Render backend URL.
// Example: window.FULLCOUNT_API_BASE = "https://your-render-service.onrender.com";
window.FULLCOUNT_API_BASE = "";
